input {
  redis {
    host => "localhost"
    port => 16379
    type => "redis-input"
    data_type => "list"
    key => "logstash"
    message_format => "json_event"
  }
}

filter {

}

output {
  elasticsearch {
    cluster => "elasticsearch"
  }
}

